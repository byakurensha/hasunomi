# User Templates

Each reusable user template gets its own folder containing `TEMPLATE.md` and
its editable source. Supplied templates live under lowercase `system/`; updates
never overwrite this user folder or force new templates to use their font,
layout, language, or production method. Bundled sample text is Japanese. Internal
slugs, JSON fields, and command names remain English.
