# User Templates

Each reusable user template gets its own folder containing `TEMPLATE.md` and
its editable source. The supplied standard A4 template lives under lowercase
`system/`; updates never overwrite this user folder or force new templates to
use its font, layout, or production method.
